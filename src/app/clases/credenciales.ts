export class Credenciales {
  usuario_cred: string;
  contrasena_cred: string;

  constructor(usuario_cred: string, contrasena_cred: string) {
    this.usuario_cred = usuario_cred;
    this.contrasena_cred = contrasena_cred;
  }
}
